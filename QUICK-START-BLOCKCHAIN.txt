╔══════════════════════════════════════════════════════════════╗
║         BLOCKPASS BLOCKCHAIN + IPFS INTEGRATION               ║
║              Quick Reference Card                             ║
╚══════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│ 📋 PREREQUISITES                                              │
└──────────────────────────────────────────────────────────────┘
  ✓ Node.js & npm installed
  ✓ Ganache installed and running on port 7545
  ✓ Chrome/Edge browser
  ✓ Pinata account (FREE 1GB - for IPFS backup)
    → Sign up at: https://app.pinata.cloud/

┌──────────────────────────────────────────────────────────────┐
│ 🚀 DEPLOYMENT STEPS                                           │
└──────────────────────────────────────────────────────────────┘

  1️⃣  START GANACHE
      ├─ Open Ganache app
      ├─ Click "Quickstart Ethereum"
      └─ Verify RPC Server: HTTP://127.0.0.1:7545

  2️⃣  DEPLOY SMART CONTRACT
      
      OPTION A: Hardhat Node (Recommended - Easier)
      ├─ Terminal 1: npx hardhat node
      ├─ Terminal 2: cd blockchain
      ├─ npm install
      ├─ npx hardhat compile
      └─ `npx hardhat run scripts/deploy-vault-audit-local.ts --network localhost`

      OPTION B: Ganache (GUI but needs configuration)
      ├─ cd blockchain
      ├─ npm install
      ├─ npx hardhat compile
      └─ npx hardhat run scripts/deploy-vault-audit.ts --network ganache
          (If error: See DEPLOYMENT-OPTIONS.md for Ganache setup)

      📝 COPY THE CONTRACT ADDRESS!
      Example: 0x5FbDB2315678afecb367f032d93F642f64180aa3

  3️⃣  GET PRIVATE KEY
      
      OPTION A: From Hardhat Node
      ├─ Look at the terminal running 'npx hardhat node'
      ├─ Copy ANY private key (20 accounts shown)
      └─ Example: ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

      OPTION B: From Ganache
      ├─ In Ganache, click the 🔑 icon next to any account
      └─ Copy the private key (64 characters, no 0x)

  4️⃣  CONFIGURE BLOCKCHAIN
      ├─ Open BlockPass extension
      ├─ Go to Settings → Blockchain Audit
      ├─ Click "⚙️ Configure Blockchain"
      ├─ Paste Contract Address
      ├─ Paste Private Key
      ├─ Click "💾 Save Configuration"
      └─ Toggle "Enable Blockchain Audit Logs" ON

  5️⃣  CONFIGURE IPFS (OPTIONAL but recommended)
      ├─ In Settings → IPFS Backup section
      ├─ Click "⚙️ Configure IPFS"
      ├─ Paste your Pinata JWT token
      │  (Get it at: https://app.pinata.cloud/developers/api-keys)
      ├─ Click "💾 Save IPFS Configuration"
      ├─ Click "🧪 Test Connection"
      └─ Toggle "Enable IPFS Decentralized Backup" ON
      
      📚 Full IPFS guide: See PINATA-SETUP-GUIDE.md

  6️⃣  TEST IT
      ├─ Save a password in BlockPass
      ├─ Open browser console (F12)
      ├─ Look for: "✅ Transaction confirmed in block: X"
      ├─ If IPFS enabled: "✅ Vault uploaded to Pinata IPFS"
      ├─ In Ganache → Transactions tab
      └─ You should see your transaction!

┌──────────────────────────────────────────────────────────────┐
│ ⚙️  CONFIGURATION                                             │
└──────────────────────────────────────────────────────────────┘

  HARDHAT NODE:
  📍 RPC URL:           http://127.0.0.1:8545
  📍 Chain ID:          31337
  📍 Accounts:          20 accounts with 10,000 ETH each

  GANACHE:
  📍 RPC URL:           http://127.0.0.1:7545
  📍 Chain ID:          1337 (Ganache default)
  
  COMMON:
  📍 Contract:          VaultAudit.sol (with IPFS CID storage)
  📍 Solidity:          ^0.8.0 (compiled with 0.8.19)
  📍 IPFS Gateway:      https://gateway.pinata.cloud/ipfs
  📍 Pinata Dashboard:  https://app.pinata.cloud/

┌──────────────────────────────────────────────────────────────┐
│ 🧪 TESTING COMMANDS                                           │
└──────────────────────────────────────────────────────────────┘

  # Compile contract
  npx hardhat compile

  # Deploy to Hardhat node (Recommended)
  npx hardhat node  # Terminal 1 - keep running
  npx hardhat run scripts/deploy-vault-audit-local.ts --network localhost

  # Deploy to Ganache (Alternative)
  npx hardhat run scripts/deploy-vault-audit.ts --network ganache

  # Run tests
  npx hardhat test

  # Clean build artifacts
  npx hardhat clean

┌──────────────────────────────────────────────────────────────┐
│ 🐛 TROUBLESHOOTING                                            │
└──────────────────────────────────────────────────────────────┘

  ❌ "Network error" / "Connection refused"
     → Hardhat: Make sure 'npx hardhat node' is running
     → Ganache: Check Ganache is running on port 7545

  ❌ "Insufficient funds" (Ganache only)
     → See DEPLOYMENT-OPTIONS.md for mnemonic configuration

  ❌ "Invalid opcode" (FIXED)
     → We updated to Solidity 0.8.19 (Ganache-compatible)
     → Run: npx hardhat clean && npx hardhat compile

  ❌ "Blockchain module not loaded"
     → Check internet connection (ethers.js CDN)
     → Reload extension

  ❌ "Transaction reverted"
     → Verify contract address is correct
     → Ensure account has ETH

  ❌ Audit logs not showing
     → Toggle "Enable Blockchain Audit Logs" ON
     → Save a password after enabling
     → Click "📊 View Audit History"

  ❌ IPFS upload failed
     → Check Pinata JWT token is correct
     → Click "🧪 Test Connection" to verify
     → See PINATA-SETUP-GUIDE.md for details

  ❌ IPFS recovery failed
     → Ensure you have correct 12-word mnemonic
     → Check CID is valid (starts with "bafy..." or "Qm...")
     → Wait 1-2 minutes for IPFS propagation
     → Try manual download: https://gateway.pinata.cloud/ipfs/YOUR_CID

  💡 TIP: Use Hardhat Node (Option A) - it's easier!
      See DEPLOYMENT-OPTIONS.md for detailed comparison.

┌──────────────────────────────────────────────────────────────┐
│ 📊 WHAT GETS LOGGED                                           │
└──────────────────────────────────────────────────────────────┘

  ✅ LOGGED TO BLOCKCHAIN:
     • Keccak256 hash of encrypted vault (64 chars)
     • IPFS Content ID (CID) if backup enabled
     • Your Ethereum address
     • Block timestamp
     • Operation type (password_added, password_updated, etc.)

  ✅ UPLOADED TO IPFS (if enabled):
     • Encrypted vault (AES-256-GCM encrypted)
     • Encryption metadata (IV, salt)
     • Returns permanent CID (e.g., bafybeig...)

  ❌ NOT LOGGED OR UPLOADED:
     • Passwords (plain or individually encrypted)
     • Usernames
     • Recovery mnemonic
     • PIN code

  🔒 PRIVACY: Only hash + CID are public. Encrypted vault on IPFS 
             requires your mnemonic to decrypt.

┌──────────────────────────────────────────────────────────────┐
│ 📁 KEY FILES                                                  │
└──────────────────────────────────────────────────────────────┘

  blockchain/
    ├─ contracts/VaultAudit.sol        (Smart contract with IPFS)
    ├─ scripts/deploy-vault-audit.ts   (Deploy script)
    └─ hardhat.config.ts               (Network config)

  extension/
    ├─ blockchain.js                   (Blockchain integration)
    ├─ ipfs.js                         (Storacha IPFS integration)
    ├─ popup.js                        (Logging logic)
    └─ popup.html                      (UI)

┌──────────────────────────────────────────────────────────────┐
│ 🎯 VERIFICATION & RECOVERY EXAMPLE                            │
└──────────────────────────────────────────────────────────────┘

  VERIFICATION:
  1. Save password → generates hash (e.g., 0xabc123...)
  2. Hash + CID logged to blockchain at timestamp T1
  3. Later: load vault → compute hash again
  4. Fetch blockchain log → compare hashes
  5. Match = ✅ Vault authentic
     Mismatch = ❌ Vault tampered!

  RECOVERY (from IPFS):
  1. Lost device? Install BlockPass on new device
  2. Click "📥 Restore from IPFS"
  3. Extension fetches latest CID from blockchain
  4. Downloads encrypted vault from Pinata
  5. Enter 12-word mnemonic → Vault decrypted!
  6. All passwords restored ✅

┌──────────────────────────────────────────────────────────────┐
│ 💡 PRO TIPS                                                   │
└──────────────────────────────────────────────────────────────┘

  BLOCKCHAIN:
  • Keep Ganache running while using BlockPass
  • Each save creates a new blockchain transaction
  • View full audit trail in Settings → View Audit History
  • Console (F12) shows detailed blockchain activity
  • Transaction history visible in Ganache "Transactions" tab

  IPFS BACKUP (Pinata):
  • Enable IPFS for automatic vault recovery capability
  • Write down 12-word mnemonic on PAPER (not digital)
  • Test recovery flow immediately after setup
  • Monitor last backup time in Settings
  • View uploads at: https://app.pinata.cloud/
  • 1GB FREE = tens of thousands of backups!

┌──────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION                                              │
└──────────────────────────────────────────────────────────────┘

  • PINATA-SETUP-GUIDE.md         (IPFS backup setup - START HERE!)
  • BLOCKCHAIN-SETUP-GUIDE.md     (Full blockchain setup)
  • DEPLOYMENT-OPTIONS.md         (Hardhat vs Ganache)
  • Contract: blockchain/contracts/VaultAudit.sol

┌──────────────────────────────────────────────────────────────┐
│ ⚡ QUICK DEPLOY (ONE-LINER)                                   │
└──────────────────────────────────────────────────────────────┘

  OPTION A: Hardhat Node (Recommended)
  Terminal 1: cd blockchain && npx hardhat node
  Terminal 2: cd blockchain && npx hardhat run scripts/deploy-vault-audit-local.ts --network localhost

  OPTION B: Ganache
  cd blockchain && npx hardhat run scripts/deploy-vault-audit.ts --network ganache

  Then copy the contract address and configure in extension!

╔══════════════════════════════════════════════════════════════╗
║  🎉 READY FOR BLOCKCHAIN + IPFS DECENTRALIZED BACKUP! 🎉     ║
║                                                               ║
║  📚 For IPFS setup, see PINATA-SETUP-GUIDE.md (5 min)        ║
║  📚 For detailed setup options, see DEPLOYMENT-OPTIONS.md    ║
╚══════════════════════════════════════════════════════════════╝
